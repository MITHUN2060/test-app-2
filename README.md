# Test App 2

Node.js starter project (`test-app-2`) with [dotenv](https://www.npmjs.com/package/dotenv) for loading environment variables from a `.env` file.

## Requirements

- [Node.js](https://nodejs.org/) (npm included)

## Setup

```bash
npm install
```

Create a `.env` file in the project root if you need secrets or config:

```env
# Example
# API_KEY=your-key-here
```

## Scripts

| Script | Description |
|--------|-------------|
| `npm test` | Placeholder (no tests yet) |

Entry point in `package.json` is `index.js`.

## License

ISC
